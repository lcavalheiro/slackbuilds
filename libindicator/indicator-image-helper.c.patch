--- libindicator/indicator-image-helper.c	2012-09-19 12:38:05.105915564 -0300
+++ libindicator/indicator-image-helper_new.c	2016-03-03 23:23:57.100366444 -0300
@@ -69,7 +69,7 @@
 	GdkPixbuf * pixbuf = gdk_pixbuf_new_from_file(icon_filename, &error);
 
 	if (icon_info != NULL) {
-		gtk_icon_info_free(icon_info);
+		g_object_unref(icon_info);
 	}
 
 	if (pixbuf == NULL) {
